# Core dependencies
pytest>=7.4.0

# Environment and configuration
python-dotenv>=1.0.0

# HTTP requests for API calls
requests>=2.31.0

# OpenAI API for embeddings and generation
openai>=1.0.0

# Anthropic API for Claude models
anthropic>=0.40.0

# NLP and NER (pin to compatible version)
spacy>=3.5.0,<3.8.0

# Machine learning
numpy>=1.24.0
scikit-learn>=1.3.0

# Future dependencies:
# - beautifulsoup4 (for HTML parsing)
# - chromadb or faiss-cpu (for vector store)
# - youtube-transcript-api (for YouTube captions)
